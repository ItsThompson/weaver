{
  "name": "weaver",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names server,client --prefix-colors blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "test": "npm test --prefix server && npm test --prefix cli && bash hooks/weaver-log.test.sh && bash bin/weaver.test.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
