# Progress

## Status

| Step | Description | Status |
|------|-------------|--------|
| 1 | Shared Types and Paths | ✅ Done |
| 2 | Config Validation for Dictation Fields | ✅ Done |
| 2.1 | Generate PR Description for voice-to-text/foundation | ✅ Done |
| 3 | Snippets Storage Service | ✅ Done |
| 4 | Snippets API Routes | ✅ Done |
| 5 | Snippet Matching Logic | ✅ Done |
| 16 | Client: Snippets Page | ✅ Done |
| 16.1 | Generate PR Description for voice-to-text/snippets | ✅ Done |
| 6 | Whisper Server Lifecycle Service | ✅ Done |
| 7 | Ollama Client Service | ✅ Done |
| 8 | Dictation History Service | ✅ Done |
| 9 | Model Download Service | ✅ Done |
| 10 | Dictation API Routes | ✅ Done |
| 10.1 | Generate PR Description for voice-to-text/dictation-services | ✅ Done |
| 11 | Build Script for whisper-server Binary | ✅ Done |
| 12 | Electron Main Process: Whisper Binary Path and F4 Shortcut | ✅ Done |
| 12.1 | Generate PR Description for voice-to-text/build-and-desktop | ✅ Done |
| 13 | Client: AudioWorklet and useAudioCapture Hook | ✅ Done |
| 14 | Client: useDictation Hook | ✅ Done |
| 15 | Client: Dictation Page Components | ✅ Done |
| 17 | Client: Model Download UI | ✅ Done |
| 20 | Client: Dictation Sounds | ✅ Done |
| 20.1 | Generate PR Description for voice-to-text/dictation-ui | ✅ Done |
| 18 | Client: Settings Page Dictation Section | ✅ Done |
| 19 | Client: Sidebar, Command Palette, and Routing | ✅ Done |
| 21 | Client: F4 IPC Integration | ✅ Done |
| 21.1 | Generate PR Description for voice-to-text/integration | ✅ Done |

## Completed tasks
- **Step 1 (Shared Types and Paths)**: Added `DictationConfig`, `Snippet`, `DictationLogEntry`, `WhisperModel` types. Added `dictation` field to `WeaverConfig` with defaults in `DEFAULT_CONFIG`. Added `snippetsPath()`, `dictationsPath()`, `modelsDir()` path helpers. All types re-exported from `@weaver/shared/types`. `turbo build` passes (6/6 tasks), all 46 shared tests pass. Committed on `voice-to-text/foundation` branch.

- **Step 2 (Config Validation for Dictation Fields)**: Added `validateDictation` function to `server/src/services/config/validators/field.ts` and registered it in `FIELD_VALIDATORS`. Validates `dictation` is an object, `ollama_url` and `ollama_model` are non-empty strings, unknown keys are ignored, missing keys merge with `DEFAULT_CONFIG.dictation`. Added 12 tests in `field.test.ts`. All 407 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/foundation` branch.

- **Step 2.1 (PR Description for voice-to-text/foundation)**: Wrote `foundation-pr-description.md` to repo root covering all 3 commits on the foundation branch (steps 1, 2, 3). Includes summary, per-step details, files changed table, testing status, and branch strategy context. Not committed per instructions.

- **Step 3 (Snippets Storage Service)**: Created `server/src/services/snippets/snippets.ts` with `readSnippets()`, `writeSnippet()`, `updateSnippet()`, `deleteSnippet()` functions. Uses `parseJsonlFile` for reads, `appendFile` for writes, `atomicWriteFile` for updates/deletes, `crypto.randomUUID()` for ID generation. Fixed missing barrel exports in `shared/paths/index.ts` for `snippetsPath`, `dictationsPath`, `modelsDir` (previous agent added them to `paths.ts` but not the barrel). Created 8 tests covering all CRUD operations. All 415 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/foundation` branch.

- **Step 4 (Snippets API Routes)**: Created `server/src/routes/snippets/snippets.ts` with `registerSnippetRoutes` function implementing GET/POST/PUT/DELETE `/api/snippets`. Manual input validation (no zod schemas) for trigger/expansion fields. Created barrel export `index.ts` and 10 BDD tests covering all scenarios (empty list, list all, create 201, missing trigger 400, missing expansion 400, update, update 404, delete 204, delete 404, PUT missing trigger 400). Registered routes in `server/src/index.ts`. All 425 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/snippets` branch.

- **Step 5 (Snippet Matching Logic)**: Created `server/src/services/dictation/snippet-matcher.ts` with `matchSnippet(transcript, snippets)` pure function. Algorithm: normalize to alpha-only lowercase, exact match, return snippet only if exactly one match (null otherwise). Created 7 TDD tests covering exact match, case-insensitive, non-alpha ignored, no match, substring no match, multiple matches, empty transcript, empty snippets. All 432 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/snippets` branch.

- **Step 6 (Whisper Server Lifecycle Service)**: Created `server/src/services/dictation/whisper-server.ts` with `startWhisperServer(binPath, modelPath)`, `stopWhisperServer()`, `isWhisperServerRunning()`, `touchWhisperActivity()`, and `WHISPER_PORT = 8178`. Uses `spawn()` from `child_process`, SIGTERM→SIGKILL after 2s stop pattern (matching `desktop/src/server.ts`), health check via `fetch` to `/health`, 5-minute inactivity timeout. Created 15 TDD tests with mocked `child_process` and `fetch`. Barrel export at `server/src/services/dictation/index.ts`. All 430 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-services` branch.

- **Step 16 (Client: Snippets Page)**: Created full Snippets page with CRUD UI on `voice-to-text/snippets` branch. Files created: `SnippetsPage.tsx` (thin orchestrator), `types.ts` (SnippetCardProps, SnippetFormProps), `hooks/useSnippetsPage.ts` (fetch/create/update/delete with SWR), `hooks/useSnippetsPage.test.tsx` (8 hook tests), `components/SnippetCard.tsx` (card with trigger, expansion, edit/delete, expand/collapse for multi-line), `components/SnippetForm.tsx` (inline form with trigger input, expansion textarea, validation), `SnippetsPage.test.tsx` (8 component tests covering empty state, guidance text, rendering cards, add form, validation, create, delete, edit pre-population), `index.ts` (barrel export). Also added: snippet CRUD functions to `client/src/utils/api.ts` (`getSnippets`, `createSnippet`, `updateSnippet`, `deleteSnippetApi`), `useSnippetsQuery`/`revalidateSnippets` to `hooks/queries/queries.ts` and barrel, snippet mocks to `__tests__/mocks/api.ts`. All 271 client tests pass (42 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/snippets` branch.

- **Step 16.1 (PR Description for voice-to-text/snippets)**: Wrote `snippets-pr-description.md` to repo root covering all 3 commits on the snippets branch (steps 4, 5, 16). Includes summary, per-step details, files changed table, testing status, and branch strategy context. Not committed per instructions.

- **Step 7 (Ollama Client Service)**: Created `server/src/services/dictation/ollama-client.ts` with `checkOllamaHealth(url)` and `generateText(url, model, prompt)` functions. `checkOllamaHealth` GETs `{url}/api/tags` and returns boolean. `generateText` POSTs to `{url}/api/generate` with `{ model, prompt, stream: false }` and returns the `response` field; non-200 responses and network errors return descriptive error strings. Created 6 TDD tests with mocked `fetch` covering success, HTTP errors, and network failures for both functions. Updated barrel export at `server/src/services/dictation/index.ts`. All 436 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-services` branch.

- **Step 8 (Dictation History Service)**: Created `server/src/services/dictation/history.ts` with `logDictation(entry: DictationLogEntry)` function. Uses `appendFile` to append JSON lines to `dictationsPath()`, creates parent directory with `mkdir({ recursive: true })` if needed. Created 2 tests covering single append and multiple appends. Updated barrel export at `server/src/services/dictation/index.ts`. All 438 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-services` branch.

- **Step 9 (Model Download Service)**: Created `server/src/services/dictation/model-download.ts` with `AVAILABLE_MODELS` constant (Tiny/Base/Small English whisper models from HuggingFace), `downloadModel(model, onProgress)` with progress tracking via `response.body.getReader()`, skip-if-exists via `stat()`, tmp file + rename for atomicity, `unlink` cleanup on failure. `listLocalModels()` returns `.bin` filenames from `modelsDir()`, `getDefaultModelPath()` returns path to first model or `null`. Created 9 BDD tests covering: directory creation + download, progress percentage callbacks, skip existing model, network error cleanup, stream read error cleanup, list .bin files, empty directory, default model path, and null when no models. Updated barrel export. All 447 server tests pass, `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-services` branch.

- **Step 10 (Dictation API Routes)**: Created `server/src/routes/dictation/dictation.ts` with `registerDictationRoutes(server, whisperBinPath)` implementing 5 routes: `GET /api/dictation/status` (pre-flight check returning whisper/ollama health + default model path), `POST /api/dictation/transcribe` (accepts raw audio buffer, starts whisper-server on demand, proxies to whisper-server as multipart/form-data), `POST /api/dictation/process` (snippet matching then Ollama cleanup, logs to dictation history), `GET /api/dictation/models` (returns available + local models), `POST /api/dictation/models/download` (SSE stream of progress events). Includes inline `matchSnippet` function (same algorithm as Step 5's snippet-matcher, which lives on Branch 2). Created barrel export `index.ts` and 10 BDD tests. Registered routes in `server/src/index.ts` with `process.env.WEAVER_WHISPER_BIN`. All 457 server tests pass (48 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-services` branch.

- **Step 10.1 (PR Description for voice-to-text/dictation-services)**: Wrote `dictation-services-pr-description.md` to repo root covering all 5 commits on the dictation-services branch (steps 6, 7, 8, 9, 10). Includes summary, per-step details, files changed table, testing status, and branch strategy context. Not committed per instructions.

- **Step 11 (Build Script for whisper-server Binary)**: Created `scripts/build-whisper.sh` that clones whisper.cpp (depth 1), builds whisper-server with CMake for arm64 macOS (`-DCMAKE_OSX_ARCHITECTURES=arm64`), copies binary to `desktop/resources/whisper-server`, and cleans up. Script is idempotent (skips if binary exists). Added `build:whisper` script to root `package.json` and integrated into `dist` pipeline. Added `whisper-server` to `extraResources` in `desktop/package.json`. Added `.whisper-build/` and `desktop/resources/whisper-server` to `.gitignore`. `turbo build` passes (6/6 tasks). Committed on `voice-to-text/build-and-desktop` branch.

- **Step 12 (Electron Main Process: Whisper Binary Path and F4 Shortcut)**: Created `desktop/src/dictation.ts` with F4 state machine (idle → recording → processing → idle), IPC handlers for `dictation-start`, `dictation-stop`, `dictation-complete`, `dictation-error`, `copy-clipboard`, `show-notification`, and native macOS `Notification` integration. Updated `desktop/src/server.ts` to pass `WEAVER_WHISPER_BIN` env var to the forked server process (packaged: `process.resourcesPath/whisper-server`, dev: `__dirname/../resources/whisper-server`). Updated `desktop/src/preload.ts` with 5 new bridge methods: `startDictation`, `stopDictation`, `onDictationCommand`, `copyToClipboard`, `showNotification`. Registered F4 global shortcut and called `setupDictation()` in `desktop/src/main.ts`. Updated `WeaverBridge` type in `client/src/types/weaver-bridge.d.ts` to match new preload methods. All 255 client tests pass (40 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/build-and-desktop` branch.

- **Step 12.1 (PR Description for voice-to-text/build-and-desktop)**: Wrote `build-and-desktop-pr-description.md` to repo root covering both commits on the build-and-desktop branch (steps 11, 12). Includes summary, per-step details, files changed table, testing status, and branch strategy context. Not committed per instructions.

- **Step 13 (Client: AudioWorklet and useAudioCapture Hook)**: Created `client/public/audio-processor.js` (AudioWorklet processor that buffers PCM samples and posts ~4s chunks via `postMessage`, supports `flush` command for final chunk on stop), `client/src/hooks/useAudioCapture/wav-encoder.ts` (pure function encoding Float32Array PCM at 16kHz to WAV Blob with 44-byte header + 16-bit samples), `client/src/hooks/useAudioCapture/useAudioCapture.ts` (React hook managing AudioContext lifecycle, mic capture via `getUserMedia`, AudioWorklet connection, linear interpolation resampling if device rate differs from 16kHz, WAV encoding of chunks, AudioContext suspend on stop), `client/src/hooks/useAudioCapture/wav-encoder.test.ts` (6 tests: blob type, file size, RIFF/WAVE header fields, sample encoding, clamping, empty input), `client/src/hooks/useAudioCapture/index.ts` (barrel export). Hook returns `{ isRecording, startRecording, stopRecording, onChunk }`. Created new branch `voice-to-text/dictation-ui` from `voice-to-text/build-and-desktop`. All 277 client tests pass (43 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-ui` branch.

- **Step 14 (Client: useDictation Hook)**: Created `client/src/hooks/useDictation/useDictation.ts` with full dictation orchestration hook implementing state machine (idle → preflight_checking → ready → recording → processing → done | error). Hook uses `useAudioCapture` for mic capture, chains transcription promises via `pendingRef` for sequential chunk processing, uses `stoppingRef` + `useEffect` watching `audio.isRecording` to detect flush completion before triggering LLM processing. Created `types.ts` with `DictationState`, `DictationActions`, `DictationPhase` types. Created `index.ts` barrel export. Added three API functions to `client/src/utils/api.ts`: `getDictationStatus` (GET /api/dictation/status), `transcribeAudio` (POST with octet-stream), `processTranscript` (POST with transcript + snippets). Added corresponding mocks to `__tests__/mocks/api.ts`. Created 9 BDD tests covering: idle initial state, healthy services → ready, ollama down → error, no whisper model → error, start → recording, chunk accumulation, stop → processing → done with LLM, F4 command → f4Active, reset → idle. All 286 client tests pass (44 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-ui` branch.

## Notes for Next Agent
- Branch `voice-to-text/integration` is now checked out with step 19 complete. The next step on this branch is step 21 (Client: F4 IPC Integration).
- DictationPage and SnippetsPage are now fully wired into routing (`/dictation`, `/snippets`), sidebar navigation, and command palette — all gated by `isElectron()`.
- In `App.tsx`, the `NAV_ITEMS` static const was replaced with a `navItems` useMemo that conditionally includes Dictation and Snippets links. Routes are conditionally rendered using `{electron && (<>...</>)}`.
- In `useWindowList.ts`, `STATIC_WINDOWS` was split into `BASE_WINDOWS` and `ELECTRON_WINDOWS`. The hook merges them conditionally based on `isElectron()`.
- The `weaver-bridge.d.ts` already had all needed types from Step 12 — no changes were needed.
- The `deleteSnippetApi` function in `api.ts` is named differently from the server-side `deleteSnippet` to avoid collision with the server import in tests. It uses raw `fetch` + manual error handling (same pattern as `deleteOrphans` and `deleteSession`).
- The hook test file is `.tsx` (not `.ts`) because it uses JSX for the SWR wrapper — matching the pattern in `useOrphansPage.test.tsx`.
- Branch `voice-to-text/build-and-desktop` is now checked out with steps 11 and 12 complete. The next step on this branch is step 12.1 (Generate PR Description for voice-to-text/build-and-desktop).
- The `build:whisper` script is integrated into the `dist` pipeline (runs before `turbo build`). It's idempotent — skips if `desktop/resources/whisper-server` already exists.
- The whisper-server binary path in the packaged app will be at `process.resourcesPath + '/whisper-server'` (electron-builder copies it from `desktop/resources/whisper-server` to the app's `Resources/whisper-server`).
- The dictation routes include an inline `matchSnippet` function duplicating the algorithm from Step 5 (on Branch 2). When branches merge, the inline version can optionally be replaced with an import from `server/src/services/dictation/snippet-matcher.ts`, but both implementations are identical so it's not strictly necessary.
- The `POST /api/dictation/transcribe` route accepts `application/octet-stream` content type (raw audio buffer) and constructs multipart/form-data to proxy to whisper-server. A custom content type parser is registered for this.
- The transcribe test has a ~1s delay due to the `setTimeout` in the whisper startup path — this is expected behavior, not a test issue.
- The `POST /api/dictation/models/download` route uses raw SSE (reply.raw.writeHead/write/end) matching the pattern in `event-bus.ts`.
- The F4 shortcut in `dictation.ts` uses `BrowserWindow.getAllWindows()[0]` to send IPC to the renderer — no changes to `window.ts` were needed.
- The `handleF4` function is the global shortcut callback; `setupDictation` registers IPC handlers. Both are called from `main.ts` during app ready.
- The `dictation-start` and `dictation-stop` IPC channels use `ipcMain.handle` (invoke pattern) while `dictation-complete`, `dictation-error`, `copy-clipboard`, and `show-notification` use `ipcMain.on` (send pattern).
- The preload `onDictationCommand` callback receives `(event, command)` where command is `"start"` or `"stop"` — the renderer will use this to drive the audio capture lifecycle.
- The `useDictation` hook uses a `stoppingRef` + `useEffect` watching `audio.isRecording` to detect when audio capture has fully stopped (including flush) before triggering LLM processing. This avoids race conditions with the async flush mechanism in `useAudioCapture`.
- Three new API functions were added to `client/src/utils/api.ts`: `getDictationStatus`, `transcribeAudio` (uses raw `fetch` with `application/octet-stream`), and `processTranscript`. Corresponding mocks added to `__tests__/mocks/api.ts`.
- The `useDictation` hook fetches snippets via `getSnippets()` at processing time (not upfront) and passes them to the `/api/dictation/process` endpoint for snippet matching.
- The hook's `copyToClipboard` action uses `window.weaver.copyToClipboard` in Electron and `navigator.clipboard.writeText` in browser.
- The preflight check considers whisper "ready" if either the server is running OR a model file exists (since whisper starts on demand). Both ollama and whisper must be ready to transition to `ready` phase.
- The `ModelDownload` component is shown on the DictationPage when `whisperStatus` is false (meaning no model exists and whisper server isn't running). It replaces `DictationControls` and `TranscriptPanel` in that state.
- The `noModel` condition in DictationPage is: `!state.whisperStatus && state.phase !== "preflight_checking" && state.phase !== "idle"` — this avoids showing ModelDownload during initial load before checkServices completes.
- When `noModel` is true, the error Alert is also hidden (since the ModelDownload component replaces the error message about missing models).
- The `ModelDownload` component's `onComplete` callback is wired to `actions.checkServices`, so after a successful download the page re-evaluates service readiness and transitions to the normal dictation view.
- The SSE download parsing uses `fetch` + `ReadableStream` reader (not `EventSource`) because the download endpoint is POST.
- The `DictationPage.test.tsx` now mocks `ModelDownload` at the module level to keep page tests focused on orchestration. The old "disables Start button when whisper is down" test was replaced with two tests: "shows ModelDownload when no model" and "calls checkServices when ModelDownload completes".
- The `getModels` API function was added to `client/src/utils/api.ts` and its mock to `__tests__/mocks/api.ts`.
- For Cloudscape `ProgressBar`, the percentage text appears in multiple DOM elements. Tests should check for the label text (e.g., "Downloading filename") rather than the percentage string to avoid "multiple elements" errors.
- The DictationPage is not yet wired into routing or sidebar — that happens in Step 19 (Client: Sidebar, Command Palette, and Routing) on the `voice-to-text/integration` branch.
- The DictationPage tests mock `useDictation` at the module level (`vi.mock("../../hooks/useDictation", ...)`) to avoid audio capture browser API dependencies. The mock returns a mutable `mockState` object and `mockActions` with `vi.fn()` stubs.
- For Cloudscape button disabled assertions, use `getByRole('button', { name })` instead of `getByText()` — `getByText` finds the inner span, not the button element, so `.toBeDisabled()` fails.
- The Cloudscape `Spinner` component does not render children as text. To show "Processing..." alongside a spinner, use `<Box><Spinner /> Processing...</Box>`.
- The `DictationControls` component uses `Button variant="link" href="/snippets"` for the "Manage Snippets" link — this renders as a Cloudscape link-styled button.

- **Step 15 (Client: Dictation Page Components)**: Created full Dictation page with component decomposition on `voice-to-text/dictation-ui` branch. Files created: `DictationPage.tsx` (thin orchestrator with Header, F4 Alert, error Alert, PreflightCheck, DictationControls, TranscriptPanel), `types.ts` (PreflightCheckProps, TranscriptPanelProps, DictationControlsProps), `hooks/useDictationPage.ts` (wraps useDictation + calls checkServices on mount), `components/PreflightCheck.tsx` (StatusIndicator for Whisper and Ollama), `components/TranscriptPanel.tsx` (two read-only Textarea fields for raw transcript and processed output, Spinner + "Processing..." text during processing phase), `components/DictationControls.tsx` (Start/Stop Dictation button, Copy to Clipboard button, Manage Snippets link), `DictationPage.test.tsx` (10 BDD tests: checkServices on mount, enabled Start + green indicators when healthy, disabled Start + error Alert when whisper down, raw transcript + Stop button during recording, Processing indicator during processing, processed output when done, F4 disables controls + info Alert, Copy calls copyToClipboard, Start calls startDictation, Stop calls stopDictation), `index.ts` (barrel export). Tests mock `useDictation` at module level to avoid audio capture dependencies. Used `getByRole('button', { name })` for Cloudscape button disabled assertions. All 296 client tests pass (45 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-ui` branch.

- **Step 17 (Client: Model Download UI)**: Created `ModelDownload.tsx` component in `client/src/pages/DictationPage/components/` with model selection table, SSE-based download progress bar, and error handling with retry. Component fetches available/local models from `GET /api/dictation/models`, shows Cloudscape Table with model name, size, and Download button (or "Downloaded" badge for already-downloaded models). Download triggers `POST /api/dictation/models/download` and parses SSE stream via `ReadableStream` reader for progress updates. Shows Cloudscape `ProgressBar` during download. On completion, calls `onComplete` callback which triggers `checkServices` to re-evaluate dictation readiness. Added `getModels` API function to `client/src/utils/api.ts` and corresponding mock. Integrated into `DictationPage.tsx`: when `whisperStatus` is false (no model available), `ModelDownload` is shown instead of `DictationControls` and `TranscriptPanel`. Updated `DictationPage.test.tsx`: mocked `ModelDownload` component, replaced the "whisper is down" test with two new tests (shows ModelDownload when no model, calls checkServices on complete). Created 6 BDD tests in `ModelDownload.test.tsx` covering: shows available models on load, marks already-downloaded models, shows progress bar during download, calls onComplete after successful download, shows error alert when download fails, shows error when fetching models fails. All 303 client tests pass (46 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-ui` branch.

- **Step 20 (Client: Dictation Sounds)**: Added three new dictation sound types to `client/src/hooks/notifications/soundUtils.ts`: `dictation-start` (rising sweep 440Hz→660Hz, 0.15s), `dictation-stop` (falling sweep 660Hz→440Hz, 0.15s), `dictation-done` (two-note ascending chime 523Hz+784Hz, offset 0.12s). Added `generateSweep` helper function for frequency-sweeping tones. Extended `NotificationSound` type union and `SOUND_URLS` record. No existing sound behavior changed. All 303 client tests pass (46 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/dictation-ui` branch.

- **Step 20.1 (PR Description for voice-to-text/dictation-ui)**: Wrote `dictation-ui-pr-description.md` to repo root covering all 5 commits on the dictation-ui branch (steps 13, 14, 15, 17, 20). Includes summary, per-step details, files changed table, testing status, and branch strategy context. Not committed per instructions.

- **Step 18 (Client: Settings Page Dictation Section)**: Added dictation configuration section to `SettingsPage.tsx` gated by `isElectron()`. New fields: "Ollama URL" input with `Test Connection` button (calls `GET /api/dictation/status`, shows `StatusIndicator` success/error), "Ollama Model" input with description listing recommended models (phi4-mini, qwen3:1.7b, gemma3:1b with pros/cons). Fields read from and write to `config.dictation.*`. Added 7 BDD tests in `SettingsPage.test.tsx`: shows dictation fields in Electron, hides in web mode, renders default values, Test Connection success, Test Connection failure (API returns false), Test Connection failure (API throws), saves dictation config. All 310 client tests pass (46 test files), `turbo build` passes (6/6 tasks). Created `voice-to-text/integration` branch from `voice-to-text/dictation-ui`. Committed on `voice-to-text/integration` branch.

- **Step 19 (Client: Sidebar, Command Palette, and Routing)**: Wired DictationPage and SnippetsPage into the app. Modified `client/src/App.tsx`: imported `DictationPage`, `SnippetsPage`, `isElectron`, replaced static `NAV_ITEMS` with dynamic `navItems` computed via `useMemo` that conditionally includes "Dictation" and "Snippets" sidebar links when `isElectron()` is true. Added conditional `<Route>` elements for `/dictation` and `/snippets` (only rendered in Electron, so web mode shows nothing for those paths). Modified `client/src/hooks/useWindowList/useWindowList.ts`: added `isElectron` import, split static windows into `BASE_WINDOWS` and `ELECTRON_WINDOWS` (Dictation with "voice speech text" searchable text, Snippets with "triggers expansion" searchable text), conditionally merged in `useMemo`. `weaver-bridge.d.ts` already had all needed types from Step 12. All 310 client tests pass (46 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/integration` branch.

- **Step 21.1 (PR Description for voice-to-text/integration)**: Wrote `integration-pr-description.md` to repo root covering all 3 commits on the integration branch (steps 18, 19, 21). Includes summary, per-step details, files changed table, testing status, and branch strategy context. Not committed per instructions.

## Open Questions / Blockers

- **Step 21 (Client: Hotkey Dictation IPC Integration)**: Created `useHotkeyDictation` hook (renamed from `useF4Dictation` to avoid hardcoding the keybind name) in `client/src/hooks/useHotkeyDictation/`. Hook listens for `onDictationCommand` IPC events, orchestrates audio capture + transcription + LLM processing, plays sounds at each stage (`dictation-start`, `dictation-stop`, `dictation-done`), and sends results back to main process via `sendDictationComplete`/`sendDictationError` IPC. Exposes `active` boolean via `HotkeyDictationContext` React context (consumer: `useHotkeyDictationActive()`). Added `sendDictationComplete(text)` and `sendDictationError(message)` to `desktop/src/preload.ts` bridge and `WeaverBridge` type — these were needed to communicate results back to the main process (which resets its F4 state machine to idle). Mounted hook in `App.tsx` with `HotkeyDictationContext.Provider` wrapping both mini and main layouts. Used `stopping` state (not ref) to trigger React re-render when audio capture stops, ensuring the processing effect fires. Created 6 BDD tests: starts inactive, plays start sound on start command, plays stop sound on stop command, sends processed text + done sound on success, sends error on failure, skips listener when not in Electron. All 316 client tests pass (47 test files), `turbo build` passes (6/6 tasks). Committed on `voice-to-text/integration` branch.

## Notes for Next Agent
- Branch `voice-to-text/integration` is checked out with step 21 complete. The next step on this branch is step 21.1 (Generate PR Description for voice-to-text/integration).
- The hook was renamed from `useF4Dictation` to `useHotkeyDictation` to avoid hardcoding the keybind name. All exports use "hotkey" naming: `useHotkeyDictation`, `useHotkeyDictationActive`, `HotkeyDictationContext`.
- Two new methods were added to the preload bridge (`sendDictationComplete`, `sendDictationError`) and `WeaverBridge` type. These map to `ipcRenderer.send('dictation-complete', text)` and `ipcRenderer.send('dictation-error', message)` which the main process `dictation.ts` already handles (resets state to idle, copies to clipboard, shows notification).
- The existing `useDictation` hook's `f4Active` state still works independently — it sets `f4Active: true` when any dictation command is received via its own `onDictationCommand` listener. Both listeners coexist since `ipcRenderer.on` supports multiple listeners.
- The `useHotkeyDictation` hook uses its own `useAudioCapture` instance, separate from any instance in `useDictation`. No shared audio state between the two hooks.
- The `stopping` state (instead of a ref) is critical — it triggers a React re-render so the `useEffect` watching `audio.isRecording` re-evaluates after `stopRecording` synchronously sets recording to false.
- The `HotkeyDictationContext` is provided at the App level, wrapping both the mini page and main layout returns. Any component can read it via `useHotkeyDictationActive()`.

(none)
